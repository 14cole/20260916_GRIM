[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ghost-em2d"
version = "0.1.0"
description = "GHOST two-dimensional electromagnetic boundary-integral solver"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0,<3", "scipy>=1.14,<2", "psutil>=5.9"]

[project.optional-dependencies]
test = ["pytest>=8,<10"]
gui = ["PySide6>=6.6,<7", "matplotlib>=3.8,<4"]

[tool.setuptools]
include-package-data = false

[tool.setuptools.packages.find]
where = ["."]
include = ["ghost_backend*"]
exclude = ["ghost_backend.tests*", "ghost_backend.*.tests*", "ghost_backend.validation.gap_feature_isolation*"]
namespaces = true

[tool.setuptools.package-data]
"ghost_backend.twod.assembly.native" = ["*.c"]
"ghost_backend.twod.fmm.native" = ["*.json", "*.md", "*.f90"]
"ghost_backend.twod.fmm.native.vendor" = ["LICENSE", "NOTICE", "AUTHORS", "*.json"]
"ghost_backend.twod.fmm.native.vendor.common" = ["*.f"]
"ghost_backend.twod.fmm.native.vendor.helmholtz" = ["*.f"]

[tool.setuptools.exclude-package-data]
"*" = ["*.dll", "*.so", "*.dylib"]

[tool.pytest.ini_options]
testpaths = ["ghost_backend/tests"]
